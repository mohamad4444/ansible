---
- name: Download kind binary
  get_url:
    url: "https://github.com/kubernetes-sigs/kind/releases/download/v{{ versions.kind }}/kind-linux-{{ architecture }}"
    dest: /usr/local/bin/kind
    mode: "0755"
