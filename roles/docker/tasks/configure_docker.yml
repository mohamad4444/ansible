# tasks/configure_docker.yml
- name: Copy Docker daemon config
  template:
    src: daemon.json.j2
    dest: /etc/docker/daemon.json
    owner: root
    group: root
    mode: '0644'
  notify:
    - Restart Docker
  when: docker_log_driver is defined and docker_log_max_size is defined