---
- name: Download talosctl binary
  get_url:
    url: "https://github.com/siderolabs/talos/releases/download/v{{ versions.talosctl }}/talosctl-linux-{{ architecture }}"
    dest: /usr/local/bin/talosctl
    mode: "0755"
