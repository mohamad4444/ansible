---
- name: Download kustomize archive
  get_url:
    url: "https://github.com/kubernetes-sigs/kustomize/releases/download/kustomize%2Fv{{ versions.kustomize }}/kustomize_v{{ versions.kustomize }}_linux_{{ architecture }}.tar.gz"
    dest: /tmp/kustomize.tar.gz

- name: Extract kustomize
  unarchive:
    src: /tmp/kustomize.tar.gz
    dest: /usr/local/bin
    remote_src: yes

- name: Set executable permission for kustomize
  file:
    path: /usr/local/bin/kustomize
    mode: "0755"
